.venv/
venv/
env/
**/__pycache__/
**/*.pyc
MagicMirror/node_modules/
node_modules/
*.log
# Recorded gesture clips — 190 MB+ of video, keep out of git
camera/gesture_training_data/
# Dataset archives produced by camera/export_dataset.py pack
camera/gestures_*.tar.gz
camera/gestures_*.tar.gz.sha256
logs/
training/
# Staging area for clips rsynced from another machine
camera/incoming_mac/
# Pre-3-class clips, kept out of the way rather than deleted
camera/old_data/

# Per-machine camera pick from camera/collect_gesture_data.py cameras
camera/.camera_choice.json

# macOS finder droppings
.DS_Store
**/.DS_Store
gesture_system/experiment_results.json
gesture_system/sweep_results.json
gesture_system/training_run.log
gesture_system/experiments.log
